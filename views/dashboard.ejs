<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>
    <h1>This is my Dashboard <%= name %> </h1>
    <% if (gender == "male") { %>
     <h1>Hello, You are a male Child</h1>
    <% } else { %>
        <h1>Hello, You are a female Child</h1>
   <% } %>


   <% for( let index = 0; index < users.length; index++ ) { %>
    <h1> <%= index + 1  %> </h1>
    <h1> <%= users[index].firstname %> </h1>
   <h1> <%= users[index].lastname %> </h1>
   <h1> <%= users[index].email %> </h1>
   <h1> <%= users[index].password %> </h1>
   <form action="/delete/<%= users[index]._id %>" method="POST" onsubmit="return confirm('Are you sure?');">
    <button type="submit" class="btn btn-danger mt-2">Delete</button>
</form>

   <button>Edit</button>
   <% } %>


   
   <!-- <% users.map((user, index)=>{ %>
       <h1> <%= index + 1 %> </h1>
       <h1> <%= user.firstname %> </h1>
       <h1> <%= user.lastname %> </h1>
       <h1> <%= user.email %> </h1>
        
   <% }) %> -->

   
    
</body>
</html>